import { get, post } from '@/axios'
import {
  INFTFilter
} from './types'

const baseUrl = ''

// export const userLogin = (params = {}) => {
//   return post(baseUrl + '/userWallet/user/login', params)
// }
// export const getSoldNFTData = (params = {}, tokenId: string) => {
//   return get(baseUrl + '/goods/firstSell/' + tokenId, params)
// }
